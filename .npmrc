# hoist=false é o mecanismo de I4, não preferência de estilo.
# Sem hoisting, um pacote só resolve o que o próprio package.json declara:
# um import de SDK de vendor num pacote de domínio falha na resolução.
hoist=false
strict-peer-dependencies=true
auto-install-peers=false
save-exact=true
